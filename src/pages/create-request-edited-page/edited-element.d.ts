import { ElementType } from "../../utils/store";
export default function EditedElemnet({ element, tabName, showBindings }: {
    element: ElementType;
    tabName: string;
    showBindings: boolean;
}): import("react/jsx-runtime").JSX.Element;
