import { TabType } from "../../utils/store";
export default function TabContentWithDragble({ tab, tabName, rows, colums, showBindings }: {
    tab: TabType;
    tabName: string;
    rows: number;
    colums: number;
    showBindings: boolean;
}): import("react/jsx-runtime").JSX.Element;
